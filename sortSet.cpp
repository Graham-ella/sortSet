#include "head.h"
int main() {
	/*
	clock_t start, end;
	start = clock();
	end = clock();
	cout << "time = " << double(double(end) - double(start)) / CLOCKS_PER_SEC << "s" << endl;
	*/
	bubbleSort();
}